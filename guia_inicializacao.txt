Para inicializar a aplicação basta colar o seguinte comando no terminal COM O .VENV ATIVADO:

uvicorn app.main:app --reload

Quando abrir a aplicação na web, no navegador (ainda com os numeros do ip) digite:

\docs

assim, você irá para uma rota a qual poderá testar a aplicação.